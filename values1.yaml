# Namespace definition
namespace: valuestest

# Deployment vars
deployment:
  name: valuestestcentralus
  replicas: 1
  revisionHistoryLimit: 6
  image: nginx:latest
  containerPorts:
    - 5000 
  resources:
    limits:
      memory: "512Mi"
      cpu: "250m"
    requests:
      memory: "256Mi"
      cpu: "125m"

# Service vars
service:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80

# CronJob Vars
cronjob:
  deploymentName: valuestest-cronjob
  image: busybox  # Replace with the image you want for the CronJob
  schedule: "*/2 * * * *"  # Every 5 minutes, adjust as needed
  concurrencyPolicy: "Allow"  # Options: Allow, Forbid, Replace
  suspend: false  # Set to true to pause CronJob
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 1
  backoffLimit: 4
  ttlSecondsAfterFinished: 300  # Time after job finishes before cleanup
